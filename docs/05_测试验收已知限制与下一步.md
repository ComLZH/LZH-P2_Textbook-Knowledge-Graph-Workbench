# 05. 测试、验收、已知限制与下一步

更新时间：2026-09-13

2026-09-11 实施说明：美术 33 节点/28 关系反馈对应的知识点中心关系模式、语义修复、二维布局、共享避障路径和邻域交互已完成。冻结样例关系模式硬指标及交叉均为 0；教材模式硬门禁通过但仍有 21 处软性交叉。设计见[文档 18](18_知识点中心关系探索与二维自动布局优化实施规划.md)，实际证据和条件项见[文档 19](19_知识点中心关系探索与二维自动布局实施完成报告_20260911.md)。

V18 保留 50 项设计编号。实现后仍不能把“50 项设计条目”写成自动化测试数：逐项状态包含通过与条件通过，后者主要是完整 GUI E2E、故障注入、125% DPI、Qt 最终截图和安装包安全矩阵。

2026-09-13 实施说明：文档 20 对应的证据顺序、单层关系分区、有序扇出、左向右布局、完整端点/箭头门禁及批量 ELK 已达到 G20-B。美术和数学两模式硬门禁均为 0，完整回归为 102 项。V20 严格结果为 42 passed、29 not_run、1 failed，因而没有声明 G20-C 或 72 项全量通过；详见[文档 21](21_关系分区与左向右自动布局可读性优化实施完成报告_20260913.md)。

## 1. 自动化测试

```powershell
python run_project.py tests
```

该命令现在直接运行完整 pytest 集合。等价命令：

```powershell
python -m pytest -q -p no:cacheprovider tests
```

当前基线：P2 102 项通过；P4 独立单元测试 33 项通过（P4 数字沿用既有独立验证，本轮未改 P4 仓库）。

覆盖范围：

- PDF/图片 Reader。
- 页码范围和 PDF 切分回退。
- `.env` 配置读取。
- `app_info.json` 公开应用信息读取、字段级回退、损坏配置回退，以及开发者信息菜单弹窗内容。
- 工作台命令栏、空状态、快捷键、模型区折叠、API Key 显隐、全局布局切换、节点/关系/待审/待修徽标和审查数量同步。
- `textbook/目录页构建测试.pdf` 真实样例导入后的预览、已就绪状态和按钮可用性。
- PDF 默认适合宽度、`Ctrl + 滚轮` 缩放、普通滚轮隔离、按钮/快捷键、25%–400% 边界、复位和跳页保持比例。
- PDF 页码组与缩放组在三种窗口尺寸、显示全部/专注教材布局间的动态同排、换行、右对齐、越界和高度释放。
- 候选协议、证据锚点和枚举归一化。
- 章节布局、关系驱动分层、对称同层、循环压缩诊断和模型建议解析。
- HTML/G6 审查结构、combo 嵌套、真实关系完整性、方向性锚点、对称关系箭头模式和旧视图坐标失效。
- G6 中心点坐标与兼容视图坐标隔离，切换视图不相互污染。
- 无界面工作台分析、进度回调和取消门禁。
- 缺少真实模型密钥时的联网前阻断。
- 注入 fake client 后的真实模型文本/图片协议路径。
- 审核导出、日期子目录、导出清单、质量错误阻断、旧正式产物清理和分层映射写出。
- 审查会话深拷贝撤销及最大历史长度。
- 关系增删改、状态覆盖、节点引用清理和批量通过。
- 底稿、正式表和关系保留。
- P4 SQLite 导入兼容性。
- OCR 结果缓存和重复运行命中。
- 页面连续性重复页/可疑页提示且不自动重排。
- 章节清单边界识别。
- 节点保守归一化、别名映射和批次临时 ID 隔离。
- 局部关系端点映射、自环阻断和重复关系合并。
- 首轮抽取后执行文本关系整合的调用顺序。
- 工作台分阶段远程路径、页面证据、运行清单和现有底稿转换。
- v1→v2 审查文档迁移、五状态保真、结构/归属/语义/未决关系分类和跨小节全部出现保留。
- v2 11 表审查包原子保存/回读、字段基线哈希、三方合并、显式冲突裁决和过期 revision 阻断。
- 共享视图投影、多归属引用、非笛卡尔语义线、表示覆盖账本、纯函数递归几何和 G6 共享坐标。
- 逻辑归档级联、归档记录保留、兼容行单事务提交、撤销历史追加和 revision 单调。
- 发布主归属门禁、目标能力检查、计划指纹失效、正式投影与原子提交。
- 冻结正文样例的真实 Chromium DOM 包围盒检查，以及 1480×900、1180×720 截图核对。
- 关系/教材双投影、统一语义样式、未知关系安全显示、共享端口/路径/标签和证据详情。
- ELK 缺失显式回退、星状/环/无关小图候选比较、300 节点有界降级和 1000 节点无递归崩溃规模报告。
- G6 一跳/二跳入口、恶意 HTML 转义、布局 6 拖动/折叠禁用、双模式状态隔离和未来版本只读保护。
- 学科/年级/册次无静默数学回退，以及 Desktop/CLI/Web 新输入门禁。
- evidence/occurrence/关系绑定优先的步骤编号提取、冲突不强配、稳定同列有序 fanout 与真实递进边保留。
- 连通分量关系分区、孤立节点合并、稳定分区 ID、完整路径/箭头/标签边界打包和组件同步平移。
- 源/目标卡片共同避障、端口外法线、终端直段、箭头多边形、长共线和分区越界硬门禁。
- 单进程批量 ELK、逐组件检查点、错误 ID/非法结果隔离、后段超时部分结果保留和 Python 有界回退。
- `</script>`、U+2028/U+2029、非有限及超界手工坐标防护，以及 300 节点多小组件完整布局。

## 2. 尚未覆盖

- 真实大模型 API 已完成 3 页冒烟；质量基线、限流和失败恢复尚未覆盖。
- PySide6 已有组件级状态测试、Windows Qt 截图验收和本地 Edge G6 页面回放；完整桌面鼠标/键盘端到端操作仍未覆盖。
- 整册教材章节覆盖率与抽取准确率。
- 多人协作与跨进程并发写锁；本轮 revision 门禁覆盖单会话过期写入和离线三方合并。
- 规模已有 100 节点 25 次、双真实文档 5 次冷/20 次热数据；尚缺 Node 子进程 RSS、跨设备对比，以及 Windows 文件占用/磁盘不足完整故障矩阵。
- 单个高密 300/1000 节点组件只提供明确诊断；由每组不超过 12 节点/24 边的小组件构成时，可在 300/1000 总节点量下完整布局。
- 教材定位模式和美术关系模式仍允许软性交叉；关系模式是密集语义阅读的默认入口，美术当前为 12 个软性交叉。
- V20 尚有 29 个完整交互、极端文字/自环、旧分区快照或诊断图局部求解矩阵未运行；旧可覆盖 HTML 未在实施前冻结，V20-01 记为 failed 的过程性证据缺口。

## 3. 当前验收等级

| 等级 | 状态 |
| --- | --- |
| 最小输入→底稿→正式表 | 通过 |
| P2→P4 导入兼容 | 通过 |
| 小样本质量报告 | 通过 |
| 真实模型协议实现 | 已实现，未形成质量基线 |
| 真实教材在线分阶段冒烟（3 页） | 通过，两轮输出存在轻微关系数量波动 |
| 真实教材本地 OCR（3 页） | 通过 |
| OCR 缓存复用（3 页） | 通过 |
| 可选页面顺序检查（3 页） | 通过，未自动重排 |
| 分阶段抽取 fake client 编排 | 通过 |
| 整册教材生产验收 | 未通过 |
| 多用户教研生产系统 | 未实现 |

## 4. 下一阶段优先级

1. 建立一套冻结的真实教材章节基准集和人工金标准。
2. 增加真实模型离线录制响应回归和失败重试/限流测试。
3. 为图谱版本、节点变更和人工审核建立审计记录。
4. 使用章节清单与章内分批保护扩展整册教材，形成覆盖率、准确率、时延和成本报告。
5. 建立表格视图模型/适配器，继续降低 UI 控件与底稿对象的双向同步耦合。

## 5. 2026-08-15 真实教材验证事实

输入：`textbook/2025秋人教版八上数学电子课本.pdf` 第 11–13 PDF 页。

- 强制共享 OCR：三页状态均为 `ok`。
- 文本块数：34、31、35。
- 页面连续性：11→12、12→13 的本地分数均为 1.0，建议均为 `keep_order`。
- 第二次相同命令：3/3 页命中缓存。
- 在线第三方 API：在用户明确授权教材文本发送至 `https://dashscope.aliyuncs.com` 后完成两轮 `qwen3.7-plus` 测试。两轮均为 2 次文本调用、0 次图片调用、10 个节点；关系数分别为 9、8。第二轮输入约 33082 字符，8 条关系全部引用已存在节点，0 条未解析线索，1 条模型弱推断进入专家复核。
- 在线质量观察：教材证据中的“三角形的高”未形成独立节点；两轮关系数有轻微波动。因此在线冒烟通过不等于覆盖率或稳定性通过。

## 6. 完成定义

只有同时满足“整册覆盖、人工金标准、真实模型稳定性、P4 导入、版本审计和可重复测试”后，才能把 P2 从建设态 MVP 提升为生产级教材图谱构建工具。

## 7. 2026-08-22 目录结构专项验收

冻结输入：`textbook/目录页构建测试.pdf`。

人工基线：6 个章、19 个节、19 条章包含节关系。

新增验收必须覆盖：

- `contains` 到父级投影的一致性和重复去除；
- 多父级、缺失端点与层级循环；
- 待审结构的候选嵌套；
- 6 个章与 19 个节的结构角色；
- 当前章/全部目录结构原子批量通过；
- 非结构关系不被批量通过；
- 孤立节节点阻止正式导出；
- 正式图谱与分层映射层级一致；
- 真正选择 3 页测试 PDF 后的来源路径和页码可追溯；
- G6 载荷应为 6 个顶层章和 19 个嵌套节，而不是 25 个顶层章节容器。

专项验收结果：以上项目均已通过。真实链路使用共享 OCR 和第三方 API，运行清单为 3 页、7 次文本调用、0 次图片调用、约 49425 个输入字符；输出 25 个结构节点、19 条 `contains`、6 个章 combo、19 个节节点、0 个层级冲突和 0 个未解析关系线索。隔离的一键通过模拟质量检查为 0 错误，正式图谱和分层映射均按 6/19/19 基线生成。

该次在线快照另外保守保留 6 条非结构关系候选，全部为 `needs_expert_review`。后续双视图修复已取消“结构端点即隐藏”的显示规则；当前冻结底稿的 8 条递进和 4 条前置会进入双视图供教师判断，但仍不参与目录批量通过，也不计入 19 条权威层级关系。

## 8. 2026-08-23 双视图与日期导出专项验收

冻结底稿：现有 `导出测试/reviewed_draft.xlsx`，无第三方 API 重跑。

- 底稿统计为 25 个节点、31 条关系：19 条 `contains`、8 条 `progressive`、4 条 `prerequisite`。
- 标题摘要为 24 个待审节点、20 条待审关系、11 条待修关系；界面以独立“待审”“待修”徽标展示。
- 兼容视图中第十三章至第十八章按目录顺序排列，节卡片位于章节内容区，递进/前置标签及待修状态不再被卡片覆盖。
- 本地 Edge 中的 G6 实例注册 19 个节点、12 条真实边、6 个 combo；其中递进 8 条、前置 4 条、结构端点抑制 0 条，控制台 0 错误、0 警告。
- 无 API 导出回放写入 `storage/validation_20260823_dual_view_consistency/export_root/2026-08-23/`。当前质量门禁存在 1 个错误，因此目录只包含本轮 `reviewed_draft.xlsx`、`graph_quality_report.md` 和 `export_manifest.json`，没有旧正式图谱或旧分层映射。
- 完整 `compileall` 与 pytest 回归结果为 `49 passed`。

视觉证据：

- `storage/ui_validation_20260823_dual_view_fix/workbench_compatibility_1480x900.png`
- `output/playwright/g6_dual_view_fix_1480x900.png`

## 9. 2026-08-23 过程记录与语义关系专项验收

- 自动化基线更新为 `53 passed`。
- `off` 不创建目录；`summary` 不保存请求和原始响应；`full` 保存脱敏全链路。
- API Key、Authorization/Bearer、密码、token 和图片 Base64 脱敏通过。
- 分析运行编号与 schema 2 导出清单关联通过。
- 调整前真实链路定位为 25 节点/19 `contains`，6 组模型原始关系合计 19 条且全部被解析保留。
- 保守语义审计后真实链路为 25 节点/35 关系：19 包含、8 前置、4 递进、4 应用；16 条语义关系均待专家审查。
- 6 组关系补全的原始/最终数量全部一致，无类型、端点或证据引用过滤，0 warning。
- Edge 真实渲染确认 6 个章节框、非包含有向边和待审标签可见。

尚未通过的是“语义候选固定数量”和“教师人工金标准确率”；35 是本轮真实模型结果，不应被冻结为系统强制目标。下一阶段应使用录制响应和教师标注计算关系类型准确率、覆盖率与波动范围。

## 10. 2026-08-25 关系驱动左向右布局专项验收

冻结输入：`导出测试/2026-08-24/reviewed_draft.xlsx`，不调用第三方 API。

- 工作簿为 15 节点、20 关系；G6 为 13 个普通节点、2 个章节 combo 和 7 条真实语义边。
- 两个章节的关系布局均为 3 个横向层级；7 条无环方向性边全部满足 `source.x < target.x`，反向边为 0。
- 方向性边的 G6 锚点为右侧输出、左侧输入；对称关系同层并使用中性/双端箭头规则；循环关系压缩到同层并输出回边诊断。
- 布局版本从 2 升级到 3，旧手工坐标自动失效；真实 Edge 点击“恢复布局”后版本为 3、手工坐标数量为 0。
- Edge 页面实际注册 1 个 canvas，加载错误提示未出现，控制台为 0 错误、0 警告。
- Windows Qt 的 G6 与兼容视图均显示章节嵌套和统一左向右主阅读流，未发现节点或章节覆盖。
- 完整 `compileall` 与 pytest 回归为 `62 passed`。

视觉证据：

- `output/playwright/g6_relation_layout_20260825_1480x900.png`
- `storage/ui_validation_20260825_relation_layout/workbench_g6_1480x900.png`
- `storage/ui_validation_20260825_relation_layout/workbench_compatibility_1480x900.png`

## 11. 2026-09-10 建设态 v2、递归布局与发布专项验收

冻结输入：`导出测试/2026-09-08/reviewed_draft.xlsx`、同轮 `export_manifest.json` 和过程记录中的 `canonical_nodes.json`，不调用第三方 API。三项 SHA-256 与文档 17 记录完全一致。

- 迁移结果为 11 节点、2 个结构作用域、9 条出现记录、21 条关系记录；家族分布为 9 membership、1 structure、8 semantic、3 unresolved。
- 共享投影为 2 个作用域、9 张知识卡片、11 条线；9 个业务知识节点全部有显示实例，每条语义关系最多生成一条线。
- Qt/G6 逻辑几何契约错误均为 0。真实 Chromium 报告 9 个节点、2 个 combo、0 个越界或同父重叠，报告绑定文档 revision、结构指纹、逻辑坐标系和 2 像素容差。
- 1480×900 与最低支持尺寸 1180×720 的静态 G6 截图人工核对通过，教材框、卡片和右侧详情区可见。
- v2 保存回读业务指纹一致；缺基线/坏哈希/过期计划会拒绝写入，Excel 同字段冲突必须显式裁决。
- 冻结样例全部待审，发布预检正确返回 `no_accepted_nodes` 且不创建正式输出。另用已接受合成样例验证主归属确认、正式投影、清单哈希、运行目录和 `current.json` 原子提交。
- 节点/关系归档保留原记录，节点归档级联归档结构记录、出现和相关边；撤销恢复业务并产生更高 revision。
- 完整回归最终为 `78 passed`。

复现命令：

```powershell
python -B -m pytest -q
python -B scripts/validate_review_document_v2.py
```

验收报告与产物：

- `docs/17_教材归属语义分离与递归布局实施完成报告_20260910.md`
- `storage/validation_20260909_review_document_v2/validation_report.json`
- `storage/validation_20260909_review_document_v2/g6_chrome_1480x900_final.png`
- `storage/validation_20260909_review_document_v2/g6_chrome_1180x720_final.png`

## 12. 2026-09-11 关系探索与二维自动布局专项验收

冻结输入：`导出测试/2026-09-10/review_document_v2.xlsx`；SHA-256 为 `f75dd170272b7542e3ae9fb7356c9bce9cde9dc557a0202210d279bed9c52872`，未调用第三方模型。

- 关系模式 33 节点/28 关系，0 节点重叠、0 穿卡、0 歧义重线、0 标签重叠、0 失败路径、0 交叉，总耗时 337.772 ms。
- 教材模式 33 节点/28 关系/1 作用域，硬门禁为 0，仍记录 21 处软性交叉，总耗时 3119.109 ms。
- 15 条 `unresolved/contains` 显示为“包含·含义待确认”，1 条真实 `semantic/explains` 显示为“解释”。
- E1 选择 elkjs 0.12.0 主后端＋Python/NetworkX v5 受保护回退；星状 18 节点因 ELK 同列占比 94% 自动选择 Python。
- 30/100 节点稀疏网完整路由且硬门禁为 0；300/1000 节点在 106/516 ms 量级内返回明确失败边的有界诊断。
- Chrome 本地离线页在 1180×720、1480×900、150% DPI 和教材模式下退出码均为 0；DOM 为 33 节点、0 前端几何违规、0 脚本错误。
- 完整 pytest 为 `90 passed in 6.74s`；`compileall` 通过。

冻结证据位于 `storage/validation_graph_readability_20260911_release/`，完整报告见[文档 19](19_知识点中心关系探索与二维自动布局实施完成报告_20260911.md)。

## 13. 2026-09-13 关系分区、有序扇出与完整端点专项验收

真实输入不调用第三方模型：

- 美术：`导出测试/2026-09-10/review_document_v2.xlsx`，SHA-256 `f75dd170272b7542e3ae9fb7356c9bce9cde9dc557a0202210d279bed9c52872`。
- 数学：`导出测试/2026-09-12/review_document_v2.xlsx`，SHA-256 `6a57842fcceaca48bcc7fb9851b37ffb0a74a62e3f366e4a6803c36a86e9ebf5`。

最终结果：

- 美术关系模式：33 节点、28 关系、5 分区、画布 2395×1295；全部硬指标为 0，软性交叉 12，总耗时约 1261.6 ms。
- 数学关系模式：25 节点、21 关系、6 分区、画布 2366×1458；全部硬指标及软性交叉为 0，总耗时约 582.8 ms。ELK 候选出现 1 个硬违规时，系统明确采用通过门禁的 Python 结果。
- 美术教材定位：33 节点、28 关系、画布 2324×960；硬门禁为 0，软性交叉 27。
- 数学教材定位：23 节点、21 关系、画布 1078×2996；硬门禁为 0，软性交叉 8。
- 真实文档 5 次冷/20 次热：美术冷 p95 1299.081 ms、热 p95 1146.371 ms；数学冷 p95 598.797 ms、热 p95 487.892 ms。
- 100 节点 10 个链组件 25 次完整布局 p95 98.929 ms；300/1000 节点的 30/100 个小链组件均完整布局且硬门禁为 0。单个高密 100/300/1000 节点图按预算进入带失败边计数的诊断。
- Chrome DOM 实际读取美术 33/28/5、数学 25/21/6，几何违规和脚本加载错误均为 0；Qt 实际绘制对应共享路径/箭头，并在 1180×720、1480×900、100/125/150% DPI 留图。
- 最终 `compileall` 通过，完整 pytest 为 `102 passed`。

证据入口：

- `storage/validation_20260913_v20_art_final3/validation_report.json`
- `storage/validation_20260913_v20_math_final3/validation_report.json`
- `storage/validation_20260913_v20_real_benchmark_final.json`
- `storage/validation_20260913_v20_scale_release3.json`
- [文档 21 完成报告](21_关系分区与左向右自动布局可读性优化实施完成报告_20260913.md)

V20 逐项总状态为 42 passed、29 not_run、1 failed，因此当前放行等级是 G20-B，不是 G20-C。failed 对应旧可覆盖 HTML 未在实施前冻结的过程缺口；当前布局功能没有硬门禁失败。尚未覆盖项主要是完整 GUI 交互自动化、极端 Unicode/自环矩阵、旧分区快照和诊断图直接局部求解。
